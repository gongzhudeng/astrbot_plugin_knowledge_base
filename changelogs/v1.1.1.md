# v1.1.1

- Restore compatibility with AstrBot v4.27.3 FastAPI plugin Web API requests.
- Preserve the existing knowledge-base response envelope for the bundled WebUI.
- Check installed dependencies before requiring `uv` for missing-package installation.
- Skip LLM provider lookup when LLM-assisted file parsing is disabled.
- Keep existing FAISS collections, preferences, and hybrid retrieval indexes unchanged.
